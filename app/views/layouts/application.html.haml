!!!
%html
  %head
    %title MyAweApp
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags

  %body
    %p
      = link_to "Home",      root_path
      = link_to "Log In",    login_path     unless current_user.present?
      = link_to "Sign Up",   sign_up_path   unless current_user.present?
      = link_to "Dashboard", dashboard_path if     current_user.present?
      = link_to "Sign Out",  sign_out_path  if     current_user.present?

    - flash.each do |type, message|
      %div{class: "alert #{type}"}
        = message

    = yield